# GitHub Pages 배포 설정 가이드

## 📋 GitHub Pages 활성화 단계

### 1. GitHub 리포지토리 페이지 접속
https://github.com/nohyujin/hrd-skill-gap-analysis

### 2. Settings 탭 클릭
리포지토리 상단 메뉴에서 **Settings** 클릭

### 3. Pages 메뉴 선택
왼쪽 사이드바에서 **Pages** 클릭

### 4. Source 설정
- **Source**: `GitHub Actions` 선택
- 다른 설정은 기본값 유지

### 5. 배포 확인
- **Actions** 탭으로 이동
- "Build and Deploy to GitHub Pages" 워크플로우 실행 확인
- 초록색 체크마크가 표시되면 배포 완료

### 6. 배포된 사이트 접속
배포 완료 후 다음 URL에서 확인 가능:
- 🌐 https://nohyujin.github.io/hrd-skill-gap-analysis/

---

## 🔧 트러블슈팅

### 워크플로우가 실행되지 않는 경우
1. `.github/workflows/deploy.yml` 파일이 올바르게 커밋되었는지 확인
2. Actions 탭에서 워크플로우 활성화 여부 확인
3. 리포지토리 Settings → Actions → General에서 "Allow all actions" 확인

### 빌드가 실패하는 경우
1. Actions 탭에서 실패한 워크플로우 클릭
2. 에러 로그 확인
3. 주요 체크 포인트:
   - `requirements.txt` 파일 존재 여부
   - `scripts/build_stlite.py` 파일 존재 여부
   - Python 버전 호환성

### 페이지가 404 에러를 표시하는 경우
1. Settings → Pages에서 Source가 "GitHub Actions"로 설정되었는지 확인
2. 워크플로우가 성공적으로 완료되었는지 확인
3. 몇 분 후 다시 시도 (배포 반영 시간 필요)

---

## 📝 참고사항

- 첫 배포는 5-10분 정도 소요될 수 있습니다
- 이후 푸시마다 자동으로 재배포됩니다
- Stlite는 브라우저에서 실행되므로 서버 비용이 없습니다
- 초기 로딩 시 WebAssembly를 다운로드하므로 약간의 시간이 걸릴 수 있습니다

---

## 🚀 다음 단계

배포가 완료되면:
1. 온라인 데모 URL을 README.md에 추가 (이미 완료)
2. 소셜 미디어나 포트폴리오에 공유
3. 사용자 피드백 수집
4. 추가 기능 개발 (Phase 5)
