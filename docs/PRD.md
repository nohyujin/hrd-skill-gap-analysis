# Product Requirements Document (PRD)
# HRD 스킬 갭 분석 대시보드

**버전:** 1.0  
**작성일:** 2025-12-24  
**프로젝트명:** HRD Skill Gap Analysis Dashboard MVP

---

## 📌 Executive Summary

본 프로젝트는 HR 담당자와 직원들이 개인 및 조직의 스킬 갭(Skill Gap)을 시각적으로 분석하고, 데이터 기반의 교육 계획을 수립할 수 있도록 지원하는 **Python 기반 웹 대시보드 MVP**입니다.

로컬 환경에서 복잡한 서버 구축 없이 즉시 실행 가능하며, Streamlit을 활용하여 빠른 프로토타이핑과 직관적인 UI를 제공합니다.

---

## 🎯 Product Vision

### 목표 (Goals)
- HR 담당자가 조직 내 스킬 갭을 데이터 기반으로 파악하고 교육 계획을 수립할 수 있도록 지원
- 직원 개개인이 자신의 현재 역량과 목표 역량 간의 차이를 시각적으로 확인
- 복잡한 인프라 없이 로컬 환경에서 즉시 실행 가능한 경량 솔루션 제공

### 핵심 가치 제안 (Value Proposition)
- **즉시 실행 가능**: 별도의 서버나 DB 구축 없이 Python과 CSV 파일만으로 동작
- **직관적 시각화**: 레이더 차트를 통한 현재/목표 역량의 직관적 비교
- **데이터 기반 의사결정**: 스킬 갭 분석 결과를 통한 교육 우선순위 결정 지원
- **확장 가능성**: AI 추천, 부서별 비교 등 추가 기능 구현 가능한 구조

---

## 👥 Target Users

### Primary Users
1. **HRD 담당자**
   - 조직 내 직원들의 스킬 갭 분석
   - 교육 계획 수립 및 우선순위 결정
   - 부서별/직무별 역량 현황 파악

2. **일반 직원**
   - 개인의 현재 역량 수준 확인
   - 목표 역량과의 차이 파악
   - 보완 필요 역량 식별

### Secondary Users
3. **팀 리더/관리자**
   - 팀원들의 역량 현황 모니터링
   - 팀 내 교육 니즈 파악

---

## 🛠️ Technical Stack

| 구분 | 기술 | 목적 |
|------|------|------|
| **언어** | Python 3.x | 백엔드 로직 및 데이터 처리 |
| **프레임워크** | Streamlit | 웹 대시보드 UI 구현 |
| **데이터 처리** | Pandas | CSV/Excel 데이터 핸들링 |
| **시각화** | Plotly | 인터랙티브 레이더 차트 및 그래프 생성 |
| **데이터 저장** | CSV 파일 | 로컬 데이터 저장소 (DB 대체) |

---

## 📋 Core Features (MVP)

### Feature 1: 데이터 관리
**우선순위:** P0 (Must Have)

#### 요구사항
- CSV 파일 기반 스킬 데이터 저장 및 로드
- 필수 데이터 필드:
  - 사원명 (Employee Name)
  - 직무 (Job Role)
  - 스킬 항목 (Skill Item)
  - 현재 점수 (Actual Score): 1-5 척도
  - 목표 점수 (Target Score): 1-5 척도

#### 데이터 스키마
```csv
사원명,직무,스킬 항목,현재 점수 (Actual),목표 점수 (Target)
김철수,데이터 분석가,Python,3,5
김철수,데이터 분석가,SQL,4,4
김철수,데이터 분석가,통계,2,4
이영희,HRD 담당자,기획력,4,5
이영희,HRD 담당자,커뮤니케이션,5,5
```

#### Acceptance Criteria
- [ ] CSV 파일을 정상적으로 읽어올 수 있음
- [ ] 데이터 형식 오류 시 적절한 에러 메시지 표시
- [ ] 최소 1명 이상의 직원 데이터 로드 가능

---

### Feature 2: 직원 선택 인터페이스
**우선순위:** P0 (Must Have)

#### 요구사항
- 사이드바에 직원 선택 드롭다운 메뉴 제공
- 선택된 직원의 데이터만 필터링하여 표시

#### Acceptance Criteria
- [ ] 전체 직원 목록이 드롭다운에 표시됨
- [ ] 직원 선택 시 해당 직원의 데이터만 화면에 표시됨
- [ ] 직원 변경 시 실시간으로 화면 업데이트

---

### Feature 3: 스킬 갭 시각화 (레이더 차트)
**우선순위:** P0 (Must Have)

#### 요구사항
- Plotly를 활용한 인터랙티브 레이더 차트 생성
- 현재 역량과 목표 역량을 동시에 표시
- 각 스킬 항목별 점수를 레이더 차트의 축으로 표현

#### 시각화 요소
- **파란색 영역**: 현재 역량 (Actual)
- **빨간색 라인**: 목표 역량 (Target)
- **축 레이블**: 스킬 항목명
- **범위**: 0-5 척도

#### Acceptance Criteria
- [ ] 레이더 차트가 정상적으로 렌더링됨
- [ ] 현재 역량과 목표 역량이 구분되어 표시됨
- [ ] 차트에 마우스 오버 시 상세 수치 표시
- [ ] 반응형으로 화면 크기에 맞게 조정됨

---

### Feature 4: 스킬 갭 분석 결과 테이블
**우선순위:** P0 (Must Have)

#### 요구사항
- 목표 점수 > 현재 점수인 스킬만 필터링
- 스킬 항목과 갭(Gap) 수치를 테이블로 표시
- 갭이 큰 순서대로 정렬 (선택사항)

#### 계산 로직
```python
Gap = 목표 점수 (Target) - 현재 점수 (Actual)
```

#### Acceptance Criteria
- [ ] 보완이 필요한 스킬만 테이블에 표시됨
- [ ] 스킬 항목과 갭 수치가 명확하게 표시됨
- [ ] 갭이 0 이하인 스킬은 표시되지 않음

---

## 🚀 Implementation Phases

### Phase 1: 기초 데이터 설계 (Week 1)
**목표:** 데이터 구조 설계 및 샘플 데이터 생성

#### Tasks
- [ ] CSV 데이터 스키마 정의
- [ ] 샘플 데이터 생성 (`skills_data.csv`)
- [ ] 데이터 검증 로직 설계

#### Deliverables
- `skills_data.csv` 파일
- 데이터 스키마 문서

---

### Phase 2: 핵심 기능 구현 (Week 2-3)
**목표:** Streamlit 기반 대시보드 개발

#### Tasks
- [ ] Streamlit 앱 기본 구조 생성 (`app.py`)
- [ ] CSV 데이터 로드 기능 구현
- [ ] 직원 선택 UI 구현 (Sidebar)
- [ ] 레이더 차트 시각화 구현
- [ ] 스킬 갭 분석 테이블 구현

#### Deliverables
- `app.py` 파일
- 실행 가능한 MVP 대시보드

---

### Phase 3: 로컬 테스트 및 검증 (Week 4)
**목표:** 기능 검증 및 사용성 테스트

#### Tasks
- [ ] 로컬 환경에서 실행 테스트
- [ ] 다양한 데이터셋으로 검증
- [ ] UI/UX 개선사항 식별
- [ ] 버그 수정

#### Deliverables
- 테스트 결과 보고서
- 안정화된 MVP 버전

---

## ✨ Future Enhancements (Post-MVP)

### Feature 5: AI 기반 교육 추천 (Phase 4)
**우선순위:** P1 (Should Have)

#### 요구사항
- OpenAI API 연동
- 스킬 갭이 있는 항목에 대해 자동으로 교육 커리큘럼 추천
- 추천 결과를 대시보드 하단에 표시

#### 구현 예시
```python
# Gap이 발생한 스킬에 대해 ChatGPT에 질의
prompt = f"{skill_name} 스킬을 보완하기 위한 교육 커리큘럼 3개를 추천해줘"
recommendations = openai.ChatCompletion.create(...)
```

#### Acceptance Criteria
- [ ] OpenAI API 연동 완료
- [ ] 스킬별 맞춤 교육 추천 제공
- [ ] 추천 결과가 읽기 쉬운 형태로 표시됨

---

### Feature 6: 부서별 평균 비교 (Phase 5)
**우선순위:** P1 (Should Have)

#### 요구사항
- 개인 점수와 부서 평균 점수를 레이더 차트에 동시 표시
- 부서 내 상대적 위치 파악 가능

#### 시각화 요소
- **파란색 영역**: 개인 현재 역량
- **빨간색 라인**: 개인 목표 역량
- **초록색 점선**: 부서 평균 역량

#### Acceptance Criteria
- [ ] 부서별 평균 계산 로직 구현
- [ ] 3개 레이어가 레이더 차트에 표시됨
- [ ] 범례를 통해 각 레이어 구분 가능

---

### Feature 7: 데이터 업로드 기능 (Phase 6)
**우선순위:** P2 (Nice to Have)

#### 요구사항
- Streamlit의 `file_uploader` 활용
- HR 담당자가 Excel/CSV 파일을 업로드하면 즉시 분석 결과 표시
- 업로드된 파일의 형식 검증

#### Acceptance Criteria
- [ ] 파일 업로드 UI 제공
- [ ] Excel 및 CSV 형식 지원
- [ ] 잘못된 형식의 파일 업로드 시 에러 메시지 표시
- [ ] 업로드 후 즉시 분석 결과 갱신

---

## 📊 Success Metrics

### MVP 성공 지표
1. **기능 완성도**
   - 4개 핵심 기능(Feature 1-4) 100% 구현
   - 로컬 환경에서 에러 없이 실행

2. **사용성**
   - 평균 3회 이내 클릭으로 원하는 정보 접근 가능
   - 초기 로딩 시간 5초 이내

3. **데이터 정확성**
   - 스킬 갭 계산 정확도 100%
   - 차트 데이터와 테이블 데이터 일치

### 장기 성공 지표 (Post-MVP)
- 실제 HR 담당자 사용자 10명 이상 확보
- 월 활성 사용자(MAU) 50명 이상
- 사용자 만족도 4.0/5.0 이상

---

## 🚧 Technical Constraints

### 시스템 요구사항
- Python 3.7 이상
- 최소 2GB RAM
- 인터넷 연결 (패키지 설치 시)

### 제약사항
- 로컬 환경에서만 실행 (클라우드 배포 미지원)
- 동시 사용자 제한 (단일 사용자 모드)
- 실시간 협업 기능 없음
- 데이터베이스 미사용 (CSV 파일 기반)

---

## 📦 Installation & Setup

### 필수 패키지 설치
```bash
pip install streamlit pandas plotly
```

### 실행 방법
```bash
streamlit run app.py
```

### 초기 데이터 준비
1. `skills_data.csv` 파일을 프로젝트 루트에 생성
2. 정의된 스키마에 맞게 데이터 입력
3. 앱 실행 시 자동으로 데이터 로드

---

## 🎓 Business Value & Portfolio Impact

### HRD 포트폴리오로서의 가치

본 프로젝트를 완성하면 다음과 같은 역량을 증명할 수 있습니다:

1. **HR 테크 이해도**
   - HR 데이터를 단순히 보는 것에 그치지 않고 솔루션화할 수 있는 능력
   - 현업의 Pain Point를 IT 도구로 해결하는 문제 해결 능력

2. **데이터 리터러시**
   - 데이터를 가공하여 인사이트(보완 필요 역량)를 도출하는 능력
   - 시각화를 통한 효과적인 데이터 커뮤니케이션

3. **기술 역량**
   - Python 기반 데이터 분석 및 시각화
   - 웹 애플리케이션 개발 경험
   - 빠른 프로토타이핑 능력

4. **비즈니스 임팩트**
   - 조직의 교육 ROI 향상에 기여
   - 데이터 기반 의사결정 문화 조성

---

## 📝 Appendix

### A. 샘플 코드 구조

```python
import streamlit as st
import pandas as pd
import plotly.graph_objects as go

# 1. 데이터 로드
df = pd.read_csv('skills_data.csv')

st.title("🎯 HRD 스킬 갭 분석 대시보드")

# 2. 사용자 선택 (Sidebar)
user_list = df['사원명'].unique()
selected_user = st.sidebar.selectbox("직원을 선택하세요", user_list)

# 3. 데이터 필터링
user_data = df[df['사원명'] == selected_user]

# 4. 시각화 (레이더 차트)
fig = go.Figure()
fig.add_trace(go.Scatterpolar(
    r=user_data['현재 점수 (Actual)'], 
    theta=user_data['스킬 항목'], 
    fill='toself', 
    name='현재 역량'
))
fig.add_trace(go.Scatterpolar(
    r=user_data['목표 점수 (Target)'], 
    theta=user_data['스킬 항목'], 
    name='목표 역량'
))

st.plotly_chart(fig)

# 5. 스킬 갭 분석 결과 출력
user_data['Gap'] = user_data['목표 점수 (Target)'] - user_data['현재 점수 (Actual)']
gap_needed = user_data[user_data['Gap'] > 0]

st.subheader(f"🔍 {selected_user}님의 집중 보완 필요 역량")
st.table(gap_needed[['스킬 항목', 'Gap']])
```

### B. 참고 자료
- [Streamlit Documentation](https://docs.streamlit.io/)
- [Plotly Python Documentation](https://plotly.com/python/)
- [Pandas Documentation](https://pandas.pydata.org/docs/)

---

**문서 버전 관리**
- v1.0 (2025-12-24): 초기 PRD 작성
